# Fly.io configuration for ChatApp.
# Filled in when we deploy the first version.

app = "chatapp"
primary_region = "iad"

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
